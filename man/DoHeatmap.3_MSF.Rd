% Generated by roxygen2 (4.1.0.9001): do not edit by hand
% Please edit documentation in R/DoHeatmap.3_MSF.R
\name{DoHeatmap.3_MSF}
\alias{DoHeatmap.3_MSF}
\title{A function that draws a heatmap based on a matrix}
\usage{
DoHeatmap.3_MSF(mat, facsepr = NULL, colfac, dendogram = "row",
  symb = NULL, dmeth = function(x) {     cor.dist(x, abs = F) },
  hmeth = "average", cex.genes = 0.5, cexCol = 1, margins = c(5, 15),
  ColD = FALSE, main = "", scl = "row", breaks = c(seq(-2, 2, 0.1)),
  colscheme = "gray", labRow = "", ...)
}
\arguments{
\item{mat}{a matrix with genes in the rows and samples in the columns}

\item{facsepr}{a factor in the phenotype data that separates columns in the heatmap}

\item{colfac}{a factor in the phenotype data that will add colors to the top according to different levels}

\item{dendogram}{indicates if dendogram should  be added to rows, columns or both}

\item{symb}{a character vector that indicates gene symbols for each row}

\item{dmeth}{the method to measure distance between matrices rows}

\item{hmeth}{the method for the hierarchical clustering}

\item{cex.genes}{is the proportion to which genes symbols should be expanded in the graphic}

\item{cexCol}{is the proportion to which columns labels should be expanded in the graphic}

\item{margins}{is the definition of vertical and horizontal margins to plot the heatmap}

\item{ColD}{is a logical parameter that indicates if the columns should be clustered}

\item{main}{is the title for the heatmap}

\item{scl}{is an indicator if data should be scaled by row, column or both}

\item{breaks}{enable color transition at specified limits}

\item{colscheme}{is the schema for colors in the body of the heatmap}
}
\description{
a function that is supposed to draw a heatmap based on a matrix of gene expressions
}
\examples{
DoHeatmap.3_MSF(mat = exprs(eset),facsepr = mygroup1, colfac = mygroup2, symb = genesymbols, dmeth=cor.dist, hmeth='average',
cex.genes=0.5, margins = c(5,15), main='', scl = row, breaks = NULL, cexCol = 1, colscheme = 'gray')
}

